<Window x:Class="TerminologyLauncher.GUI.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ProgressWindow" Height="184" Width="592"
        Style="{DynamicResource CustomWindowStyle}"
        WindowStartupLocation="CenterScreen"

        DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles/CustomStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <DockPanel>
        <Canvas DockPanel.Dock="Bottom">
            <Canvas></Canvas>
            <ProgressBar Value="{Binding Path=Progress}"  Height="39" Canvas.Left="42" Canvas.Top="75" Width="497" Style="{DynamicResource TerminologyProgressBar}"/>
        </Canvas>

    </DockPanel>
</Window>
